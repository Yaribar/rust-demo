[package]
name = "sqlite-hf"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-bert = "0.22"
clap = { version = "4.5", features = ["derive"] }
sqlite = "0.36"
console = { version = "0.16", features = ["std"] }